{
  "name": "cluster_worker_experiment",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cluster": "node cluster.js",
    "worker_threads": "node --experimental-worker worker_threads.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sha256": "^0.2.0"
  }
}
